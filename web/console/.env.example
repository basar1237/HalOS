# HalOS Konsol — Ortam Değişkenleri (örnek)
# Gerçek .env dosyaları koda yazılmaz (docs/04 §7).

# API Gateway (BFF) taban adresi — 04 §3 mikroservis haritası
NEXT_PUBLIC_API_BASE_URL=http://localhost:5000
